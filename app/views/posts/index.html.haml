%head
  %title Joynus Staffing| News | Check out our news
      
  
- if flash[:notice]
  = render 'shared/success_notice'

%section.feature-section.no-spacing
  .feature.animated.fadeIn
    .feature-container
      %img{ data: { interchange:"[#{asset_path('news-bg-mobile.jpg')}, (small)], [#{asset_path('news-bg.jpg')}, (medium)], [#{asset_path('news-bg.jpg')}, (large)]" } }
      .feature-text
        %h1.text-center.animated.fadeInDown= t 'blog.feature-header'
.row
  .columns.large-12.large-offset-1
    .path
      %a{:href => "/"}  
        .srvIco1
          %i.icon-home
      %a{:href => "/"}
        %h5 home    
      .srvIco11
        %i.icon-right  
      %h5 News
    
- @posts.each do |post|
  .row
    .columns.large-8.large-offset-2
      .blog-posts
        = link_to post_url(post) do
          %h3.secondary-text= post.title
        %p= l post.created_at.to_date, format: :long

        %p.post-content= truncate(post.raw_post, length: 1000, separator: ' ', escape: false)

        = link_to 'View More', post_url(post), class: 'secondary-text'

= paginate @posts
